import React from 'react'
import ReactDOM from 'react-dom/client'
import LaptopPricePredictor from './LaptopPricePredictor'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LaptopPricePredictor />
  </React.StrictMode>
)
